name: Daily AI Fund Advisor (Analysis)

on:
  schedule:
    # 北京时间 14:00 (UTC 06:00)
    - cron: '0 6 * * 1-5'
  workflow_dispatch:

concurrency: 
  group: ai-analysis
  cancel-in-progress: true

jobs:
  analyze_and_report:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Shanghai

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        cache: 'pip'

    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt

    - name: 🧠 Run Analysis System
      env:
        # 分析任务需要全套密钥
        LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
        LLM_BASE_URL: ${{ secrets.LLM_BASE_URL }}
        EMAIL_USER: ${{ secrets.MAIL_USER }}  # 注意与代码环境变量名匹配
        EMAIL_PASS: ${{ secrets.MAIL_PASS }}
        EMAIL_TO: ${{ secrets.MAIL_USER }}    # 发给自己
        
      run: |
        # 此时运行 main.py，它会自动读取 data_crawler 提交的 csv 文件
        # 不会发起网络请求，速度极快
        python main.py

    - name: Commit and Push Portfolio
      run: |
        git config --global user.name "GitHub Action"
        git config --global user.email "action@github.com"
        
        # 只提交记账本的变化
        if [[ -n $(git status --porcelain portfolio.json) ]]; then
          git add portfolio.json
          git commit -m "📈 Auto-update portfolio ledger [skip ci]"
          
          # [关键] 必须rebase，因为爬虫任务可能刚刚提交了数据
          git pull --rebase origin main
          git push
        else
          echo "No changes in portfolio."
        fi
