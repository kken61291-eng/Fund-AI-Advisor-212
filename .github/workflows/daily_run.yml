name: Daily AI Fund Advisor (Analysis)

on:
  schedule:
    # 北京时间 14:00 (UTC 06:00) 执行分析
    - cron: '0 6 * * 1-5'
  workflow_dispatch:

concurrency: 
  group: ai-analysis
  cancel-in-progress: true

jobs:
  analyze_and_report:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Shanghai

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        cache: 'pip'

    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt

    - name: 🧠 Run Analysis System
      env:
        # --- 核心修改：变量映射 ---
        # 左边是 Python 代码(utils.py)里要求的名字
        # 右边是你 GitHub 仓库(截图)里的 Secret 名字
        EMAIL_USER: ${{ secrets.MAIL_USER }}
        EMAIL_PASS: ${{ secrets.MAIL_PASS }}
        EMAIL_TO: ${{ secrets.MAIL_USER }}   # 发给自己
        
        # 其他变量
        LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
        LLM_BASE_URL: ${{ secrets.LLM_BASE_URL }}
        
      run: |
        # 运行主程序，读取本地数据进行分析
        python main.py

    - name: Commit and Push Portfolio
      run: |
        git config --global user.name "GitHub Action"
        git config --global user.email "action@github.com"
        
        if [[ -n $(git status --porcelain portfolio.json) ]]; then
          git add portfolio.json
          git commit -m "📈 Auto-update portfolio ledger [skip ci]"
          git pull --rebase origin main
          git push
        else
          echo "No changes in portfolio."
        fi
